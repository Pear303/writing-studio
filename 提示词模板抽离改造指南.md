# 流水线写作提示词模板抽离改造指南

> **文档版本**: v1.0  
> **创建日期**: 2026-05-31  
> **适用范围**: writing-studio-private 项目  
> **目标**: 将硬编码在 `NovelLLMService.ts` 中的提示词抽离为独立的 Markdown 模板文件，实现热拔插、按需加载和灵活管理

---

## 📋 目录

1. [现状分析](#现状分析)
2. [改造目标](#改造目标)
3. [新架构设计](#新架构设计)
4. [分步实施计划](#分步实施计划)
   - [第一步：创建目录结构](#第一步创建目录结构)
   - [第二步：编写提示词模板文件](#第二步编写提示词模板文件)
   - [第三步：扩展 SmartPromptComposer](#第三步扩展-smartpromptcomposer)
   - [第四步：重构 NovelLLMService](#第四步重构-novelllmservice)
   - [第五步：更新加载逻辑](#第五步更新加载逻辑)
   - [第六步：测试与验证](#第六步测试与验证)
5. [模板编写规范](#模板编写规范)
6. [热拔插机制实现](#热拔插机制实现)
7. [质量保证与回滚](#质量保证与回滚)
8. [常见问题解答](#常见问题解答)

---

## 现状分析

### 当前架构

项目已实现基础的分层提示词架构：

```
✅ 已实现
├── references/                  # 参考知识库文档
├── prompts-index.ts             # 阶段与提示词文件映射
├── promptBuilders.ts            # 上下文构建器
└── SmartPromptComposer          # 提示词组合引擎

❌ 未实现（需要改造）
├── NovelLLMService.ts           # 硬编码的用户提示词（user message）
└── 缺少模板渲染机制             # 无法动态替换变量
```

### 存在的问题

**NovelLLMService.ts 中的硬编码提示词示例**：

```typescript
// 问题 1：生成大纲的提示词（第 93-118 行）
const userMessage = parts.length > 0
  ? `请根据以下设定生成小说大纲：\n${parts.join('\n')}`
  : '请帮我生成小说大纲';

// 问题 2：生成细纲的提示词（第 165-203 行）
const userMessage = `你是一个小说细纲生成助手。以下是大纲内容：

【大纲】
${outline}

请根据以上大纲，生成 ${chapterCount} 个章节的细纲。每个章节细纲需包含：
1. 章节标题
2. 核心情节点（3-5个）
...`;

// 问题 3：生成正文的提示词（第 320-365 行）
const userMessage = `你是一个小说写作助手。请根据以下信息撰写章节正文。

【全书大纲概览】
${outline}

【当前章节细纲】
第${chapterIndex + 1}章：${chapterTitle}
${chapterOutline}
...`;
```

**核心问题**：
- 修改提示词需要重新编译代码
- 无法进行 A/B 测试
- 非开发人员无法编辑
- 提示词逻辑与业务代码耦合

---

## 改造目标

### 功能目标

- [x] 将 `NovelLLMService.ts` 中的 6 个核心提示词方法抽离为 Markdown 模板
- [x] 建立模板变量替换机制（支持 `{{variable}}` 语法）
- [x] 实现按需加载和缓存机制
- [x] 支持用户自定义提示词覆盖（热拔插）
- [x] 保持向后兼容性，不破坏现有功能

### 架构目标

```
改造前                    改造后
┌─────────────┐          ┌─────────────┐
│ UI 组件层   │          │ UI 组件层   │
└──────┬──────┘          └──────┬──────┘
       │                        │
┌──────▼──────┐          ┌──────▼──────┐
│ App.tsx     │          │ App.tsx     │
└──────┬──────┘          └──────┬──────┘
       │                        │
┌──────▼────────────────┐  ┌────▼────────────────┐
│ NovelLLMService.ts    │  │ NovelLLMService.ts  │
│ ❌ 硬编码提示词       │  │ ✅ 调用模板渲染引擎  │
└───────────────────────┘  └────┬────────────────┘
                                 │
                          ┌──────▼──────────────┐
                          │ TemplateRenderer    │
                          │ ✅ 变量替换         │
                          │ ✅ 缓存管理         │
                          └──────┬──────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │ src/prompts/            │
                    │ ├── templates/          │
                    │ │   ├── outline/        │
                    │ │   ├── detailed/       │
                    │ │   └── chapter/        │
                    │ ├── references/         │
                    │ └── index.ts            │
                    └─────────────────────────┘
```

---

## 新架构设计

### 目录结构

```
src/
├── prompts/                          # 🆕 提示词管理中心
│   ├── templates/                    # 🆕 可直接使用的提示词模板
│   │   ├── pipeline/                 # 流水线写作专用模板
│   │   │   ├── outline-generate.md   # Step2: 生成大纲
│   │   │   ├── outline-refine.md     # Step2: 回炉重造大纲
│   │   │   ├── detailed-generate.md  # Step4: 生成细纲
│   │   │   ├── detailed-refine.md    # Step4: 回炉重造细纲
│   │   │   ├── detailed-refine-chapter.md  # Step4: 单章回炉
│   │   │   ├── chapter-generate.md   # Step5: 生成正文
│   │   │   └── chapter-refine.md     # Step5: 回炉重造正文
│   │   └── common/                   # 通用模板
│   │       └── role-definitions.md   # 角色定义
│   ├── references/                   # 📝 现有参考文档（保留）
│   │   ├── WritingGuide/
│   │   ├── outline-template.md
│   │   └── ...
│   ├── index.ts                      # 🆕 模板索引与配置
│   └── types.ts                      # 🆕 类型定义
├── hooks/
│   ├── SmartPromptComposer.ts        # 现有组件（扩展）
│   ├── prompts-index.ts              # 现有组件（保留）
│   └── promptBuilders.ts             # 现有组件（保留）
└── llm/
    └── NovelLLMService.ts            # 重构目标
```

### 核心组件

#### 1. 模板渲染引擎（新增）

```typescript
// src/prompts/TemplateRenderer.ts
export class TemplateRenderer {
  private cache = new Map<string, string>();
  private templateDir: string;

  // 加载模板并缓存
  async loadTemplate(name: string): Promise<string> {
    if (this.cache.has(name)) {
      return this.cache.get(name)!;
    }

    const template = await this.fetchTemplate(name);
    this.cache.set(name, template);
    return template;
  }

  // 渲染模板（变量替换）
  render(template: string, variables: Record<string, any>): string {
    return template.replace(/\{\{(\w+)\}\}/g, (match, key) => {
      return variables[key] !== undefined ? variables[key] : match;
    });
  }

  // 一步完成加载和渲染
  async renderTemplate(name: string, variables: Record<string, any>): Promise<string> {
    const template = await this.loadTemplate(name);
    return this.render(template, variables);
  }

  // 清除缓存（用于热更新）
  clearCache(name?: string): void {
    if (name) {
      this.cache.delete(name);
    } else {
      this.cache.clear();
    }
  }
}
```

#### 2. 模板索引配置（新增）

```typescript
// src/prompts/index.ts
export interface PromptTemplate {
  id: string;
  name: string;
  description: string;
  file: string;
  stage: 'PLANNING' | 'DETAILED_OUTLINE' | 'CHAPTER_WRITING';
  variables: string[]; // 必需的变量名
  version: string;
}

export const PROMPT_TEMPLATES: Record<string, PromptTemplate> = {
  'outline-generate': {
    id: 'outline-generate',
    name: '生成大纲',
    description: '根据用户配置生成小说大纲',
    file: './templates/pipeline/outline-generate.md',
    stage: 'PLANNING',
    variables: ['genres', 'plotType', 'protagonistIdentity', 'tone', 'customPrompt'],
    version: '1.0.0',
  },
  // ... 其他模板配置
};
```

---

## 分步实施计划

### 第一步：创建目录结构

**执行操作**：

```bash
# 在项目根目录执行
mkdir -p src/prompts/templates/pipeline
mkdir -p src/prompts/templates/common
touch src/prompts/index.ts
touch src/prompts/types.ts
touch src/prompts/TemplateRenderer.ts
```

**预期结果**：

```
src/prompts/
├── templates/
│   ├── pipeline/
│   └── common/
├── references/ (符号链接或复制现有内容)
├── index.ts
├── types.ts
└── TemplateRenderer.ts
```

**验证检查点**：
- [ ] 目录结构创建成功
- [ ] 文件路径正确
- [ ] TypeScript 编译无错误

---

### 第二步：编写提示词模板文件

**任务清单**：

#### 2.1 创建 `outline-generate.md`

**文件路径**: `src/prompts/templates/pipeline/outline-generate.md`

**模板内容**：

```markdown
# 角色
你是一位专业的小说大纲规划师，擅长根据用户提供的题材和设定，生成结构化的小说大纲。

# 任务
请根据以下设定生成小说大纲：

{{#if genres}}
**主题题材**: {{genres}}
{{/if}}

{{#if plotType}}
**剧情类型**: {{plotType}}
{{/if}}

{{#if protagonistIdentity}}
**主角身份**: {{protagonistIdentity}}
{{/if}}

{{#if tone}}
**基调**: {{tone}}
{{/if}}

{{#if customPrompt}}
**额外要求**:
{{customPrompt}}
{{/if}}

# 输出要求
1. 大纲应包含基本信息、TODO 清单、章节规划、悬念线
2. 使用标准 Markdown 格式
3. 每个章节条目以 `- ` 开头
4. 使用两个空格缩进表示层级关系
5. 只输出大纲内容，不要额外解释

# 格式示例
```
## 基本信息
- 题材：xxx
- 基调：xxx

## 章节规划
- 第一章：开端
  - 主角发现关键线索
  - 首个冲突出现
- 第二章：发展
  - ...
```
```

#### 2.2 创建 `outline-refine.md`

**文件路径**: `src/prompts/templates/pipeline/outline-refine.md`

```markdown
# 角色
你是一位小说大纲编辑助手，擅长根据用户的反馈迭代改进大纲。

# 任务
根据用户提供的大纲迭代历史和修改要求，生成改进版大纲。

# 上下文信息

## 原始大纲
{{originalOutline}}

## 当前大纲
{{currentOutline}}

{{#if historyLines}}
## 历史修改记录
{{historyLines}}
{{/if}}

## 本轮修改要求
{{#if additions}}
**新增**: {{additions}}
{{/if}}

{{#if deletions}}
**删除**: {{deletions}}
{{/if}}

{{#if modifications}}
**修改**: {{modifications}}
{{/if}}

# 输出要求
1. 在当前大纲基础上生成改进版
2. 保持原有的结构和格式
3. 只输出改进后的大纲内容，不要额外解释
```

#### 2.3 创建其他模板文件

按照相同模式创建以下文件：

- `detailed-generate.md` - Step4 生成细纲
- `detailed-refine.md` - Step4 回炉重造细纲
- `detailed-refine-chapter.md` - Step4 单章回炉
- `chapter-generate.md` - Step5 生成正文
- `chapter-refine.md` - Step5 回炉重造正文

**每个模板文件应包含**：
1. 角色定义
2. 任务描述
3. 上下文变量占位符（使用 `{{variable}}` 语法）
4. 输出要求
5. 格式示例（可选）

**验证检查点**：
- [ ] 所有 7 个模板文件创建完成
- [ ] 模板语法正确（无未闭合的 `{{` 或 `}}`）
- [ ] 变量名与业务逻辑匹配

---

### 第三步：扩展 SmartPromptComposer

**目标**：在现有 `SmartPromptComposer` 中增加模板渲染能力

**修改文件**: `src/hooks/SmartPromptComposer.ts`

**新增方法**：

```typescript
// 在 SmartPromptComposer 类中添加
export class SmartPromptComposer {
  // ... 现有代码 ...

  // 🆕 新增：加载模板
  async loadTemplate(templateId: string): Promise<string> {
    const templateConfig = PROMPT_TEMPLATES[templateId];
    if (!templateConfig) {
      throw new Error(`未找到模板配置: ${templateId}`);
    }

    // 优先加载用户自定义模板
    const userTemplate = await this.loadUserTemplate(templateConfig.file);
    if (userTemplate) {
      return userTemplate;
    }

    // 加载内置模板
    return await this.loadBuiltInTemplate(templateConfig.file);
  }

  // 🆕 新增：渲染模板
  async renderTemplate(templateId: string, variables: Record<string, any>): Promise<string> {
    const template = await this.loadTemplate(templateId);
    return this.replaceVariables(template, variables);
  }

  // 🆕 新增：变量替换（支持简单语法）
  private replaceVariables(template: string, variables: Record<string, any>): string {
    // 处理 {{#if variable}}...{{/if}} 条件块
    template = template.replace(/\{\{#if (\w+)\}\}([\s\S]*?)\{\{\/if\}\}/g, (match, varName, content) => {
      return variables[varName] ? content.trim() : '';
    });

    // 处理 {{variable}} 简单变量
    template = template.replace(/\{\{(\w+)\}\}/g, (match, key) => {
      return variables[key] !== undefined ? String(variables[key]) : match;
    });

    return template;
  }

  // 🆕 新增：加载用户自定义模板
  private async loadUserTemplate(filePath: string): Promise<string | null> {
    // TODO: 使用 Tauri API 读取用户目录
    // const userPromptsDir = await getUserPromptsPath();
    // const fullPath = path.join(userPromptsDir, filePath);
    // return await readTextFile(fullPath);
    return null; // 暂时返回 null，使用内置模板
  }

  // 🆕 新增：加载内置模板
  private async loadBuiltInTemplate(filePath: string): Promise<string> {
    // 使用 Vite 的动态导入
    const modules = import.meta.glob('../prompts/templates/**/*.md', {
      query: '?raw',
      eager: true,
      import: 'default',
    });

    const normalizedPath = filePath.replace('./templates/', '../prompts/templates/');
    const content = modules[normalizedPath];

    if (typeof content === 'string') {
      return content;
    }

    throw new Error(`未找到模板文件: ${filePath}`);
  }

  // 🆕 新增：清除模板缓存
  clearTemplateCache(templateId?: string): void {
    // 清除 Vite 的模块缓存
    if (import.meta.hot) {
      if (templateId) {
        const config = PROMPT_TEMPLATES[templateId];
        if (config) {
          const path = config.file.replace('./templates/', '../prompts/templates/');
          import.meta.hot.invalidate(`模板已更新: ${path}`);
        }
      } else {
        import.meta.hot.invalidate('所有模板缓存已清除');
      }
    }
  }
}
```

**验证检查点**：
- [ ] TypeScript 类型检查通过
- [ ] 模板加载逻辑正确
- [ ] 变量替换功能正常
- [ ] 条件语法解析正常

---

### 第四步：重构 NovelLLMService

**目标**：将硬编码的提示词替换为模板渲染调用

**修改文件**: `src/llm/NovelLLMService.ts`

#### 4.1 重构 `generatePipelineOutline` 方法

**改造前**：

```typescript
async generatePipelineOutline(config: PipelineStep1Config): Promise<string> {
  const parts: string[] = [];
  if (config.genres.length > 0) {
    parts.push(`主题题材：${config.genres.join('、')}`);
  }
  if (config.plotType) {
    parts.push(`剧情类型：${config.plotType}`);
  }
  // ... 更多拼接逻辑 ...

  const userMessage = parts.length > 0
    ? `请根据以下设定生成小说大纲：\n${parts.join('\n')}`
    : '请帮我生成小说大纲';

  const result = await this.generate('PLANNING', userMessage, { ... });
  return result.content;
}
```

**改造后**：

```typescript
async generatePipelineOutline(config: PipelineStep1Config): Promise<string> {
  // 🆕 使用模板渲染
  const userMessage = await this.composer!.renderTemplate('outline-generate', {
    genres: config.genres.join('、'),
    plotType: config.plotType,
    protagonistIdentity: config.protagonistIdentity,
    tone: config.tone,
    customPrompt: config.customPrompt.trim(),
  });

  const result = await this.generate('PLANNING', userMessage, {
    novelType: config.genres.join(','),
    protagonistTypes: config.protagonistIdentity ? [config.protagonistIdentity] : [],
    plotTypes: config.plotType ? [config.plotType] : [],
    coreIdea: config.customPrompt.trim(),
  });

  return result.content;
}
```

#### 4.2 重构 `refinePipelineOutline` 方法

**改造后**：

```typescript
async refinePipelineOutline(
  step2State: PipelineStep2State,
  currentRound: OutlineRound,
): Promise<string> {
  // 🆕 构建历史记录
  const historyLines = step2State.rounds.map((r, i) => {
    const parts: string[] = [];
    if (r.additions.trim()) parts.push(`新增：${r.additions.trim()}`);
    if (r.deletions.trim()) parts.push(`删除：${r.deletions.trim()}`);
    if (r.modifications.trim()) parts.push(`修改：${r.modifications.trim()}`);
    return `第${i + 1}轮：${parts.join(' / ')}`;
  }).join('\n');

  // 🆕 使用模板渲染
  const userMessage = await this.composer!.renderTemplate('outline-refine', {
    originalOutline: step2State.originalOutline,
    currentOutline: step2State.currentOutline,
    historyLines: historyLines.length > 0 ? historyLines : '',
    additions: currentRound.additions.trim(),
    deletions: currentRound.deletions.trim(),
    modifications: currentRound.modifications.trim(),
  });

  const result = await this.generate('PLANNING', userMessage, {
    novelType: '',
    protagonistTypes: [],
    plotTypes: [],
    coreIdea: [
      currentRound.additions,
      currentRound.deletions,
      currentRound.modifications
    ].filter(Boolean).join('; '),
  });

  return result.content;
}
```

#### 4.3 重构其他方法

按照相同模式重构以下方法：

- [ ] `generatePipelineDetailedOutline` → 使用 `detailed-generate` 模板
- [ ] `refinePipelineDetailedOutline` → 使用 `detailed-refine` 模板
- [ ] `refinePipelineDetailedOutlineChapter` → 使用 `detailed-refine-chapter` 模板
- [ ] `generatePipelineChapter` → 使用 `chapter-generate` 模板
- [ ] `refinePipelineChapter` → 使用 `chapter-refine` 模板

**验证检查点**：
- [ ] 所有 6 个方法重构完成
- [ ] TypeScript 编译无错误
- [ ] 运行时提示词生成结果与改造前一致
- [ ] 单元测试通过（如有）

---

### 第五步：更新加载逻辑

**目标**：在 `App.tsx` 中更新提示词加载逻辑

**修改文件**: `src/App.tsx`

**修改位置**：所有调用 `novelLLMService.init()` 的地方

**改造前**：

```typescript
const prompts = new Map<string, string>();
const modules = import.meta.glob('./references/*.md', {
  query: '?raw',
  eager: true,
  import: 'default',
});
for (const [path, content] of Object.entries(modules)) {
  if (typeof content === 'string') {
    const fileName = path.split('/').pop() || '';
    prompts.set(fileName, content);
  }
}
novelLLMService.init(prompts, { ... });
```

**改造后**：

```typescript
const prompts = new Map<string, string>();

// 🆕 加载参考文档（原有逻辑）
const refModules = import.meta.glob('./references/*.md', {
  query: '?raw',
  eager: true,
  import: 'default',
});
for (const [path, content] of Object.entries(refModules)) {
  if (typeof content === 'string') {
    const fileName = path.split('/').pop() || '';
    prompts.set(fileName, content);
  }
}

// 🆕 加载提示词模板（新增）
const templateModules = import.meta.glob('./prompts/templates/**/*.md', {
  query: '?raw',
  eager: true,
  import: 'default',
});
for (const [path, content] of Object.entries(templateModules)) {
  if (typeof content === 'string') {
    const fileName = path.split('/').pop() || '';
    prompts.set(`template:${fileName}`, content);
  }
}

novelLLMService.init(prompts, {
  apiKey,
  baseUrl: defaultConfig.apiUrl.replace(/\/chat\/completions\/?$/, '').replace(/\/$/, ''),
  model: defaultConfig.model,
});
```

**验证检查点**：
- [ ] 所有 `init()` 调用点更新完成
- [ ] 参考文档和模板都正确加载
- [ ] 无重复加载或遗漏

---

### 第六步：测试与验证

#### 6.1 功能测试清单

| 测试项 | 测试步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| Step2 生成大纲 | 填写题材配置，点击"开始生成大纲" | 成功生成大纲，提示词符合模板定义 | ☐ |
| Step2 回炉大纲 | 输入新增/删除/修改要求，点击"回炉重造" | 根据要求改进大纲 | ☐ |
| Step4 生成细纲 | 输入章节数量，点击生成 | 生成指定数量的细纲 | ☐ |
| Step4 回炉细纲 | 输入修改要求，点击回炉 | 改进细纲内容 | ☐ |
| Step5 生成正文 | 选择章节，点击生成 | 生成符合细纲的正文 | ☐ |
| Step5 回炉正文 | 输入修改要求，点击回炉 | 改进正文内容 | ☐ |

#### 6.2 性能测试

```typescript
// 在浏览器控制台执行
console.time('模板加载');
await composer.loadTemplate('outline-generate');
console.timeEnd('模板加载');

console.time('模板渲染');
await composer.renderTemplate('outline-generate', { genres: '玄幻' });
console.timeEnd('模板渲染');
```

**性能目标**：
- 模板加载：< 50ms（首次），< 5ms（缓存命中）
- 模板渲染：< 10ms

#### 6.3 回归测试

- [ ] 现有功能不受影响
- [ ] 参考文档（references/）仍然正常工作
- [ ] SmartPromptComposer 原有功能正常
- [ ] 无内存泄漏（模板缓存正确管理）

---

## 模板编写规范

### 语法规范

#### 1. 简单变量替换

```markdown
**题材**: {{genres}}
**主角**: {{protagonist}}
```

**渲染结果**：
```markdown
**题材**: 玄幻、冒险
**主角**: 废柴少年
```

#### 2. 条件块

```markdown
{{#if customPrompt}}
**额外要求**:
{{customPrompt}}
{{/if}}
```

**渲染结果**（当 `customPrompt` 有值时）：
```markdown
**额外要求**:
主角必须有金手指
```

**渲染结果**（当 `customPrompt` 为空时）：
```markdown
（整个条件块被移除）
```

#### 3. 列表渲染（可选，进阶）

```markdown
**修改历史**:
{{#each rounds}}
- 第{{index}}轮: {{content}}
{{/each}}
```

> **注意**：当前版本暂不支持列表渲染，可在后续版本中扩展

### 命名规范

| 元素 | 规范 | 示例 |
|------|------|------|
| 模板文件名 | 小写字母 + 连字符 | `outline-generate.md` |
| 变量名 | 驼峰命名 | `chapterTitle`, `customPrompt` |
| 模板 ID | 与文件名一致（不含扩展名） | `outline-generate` |

### 内容结构规范

每个模板文件应包含以下部分：

```markdown
# 角色
[定义 AI 的角色和专长]

# 任务
[描述具体任务]

# 上下文信息
[使用变量占位符插入动态内容]

## 子章节
{{variable}}

# 输出要求
[列出格式、风格等要求]

# 格式示例（可选）
[提供期望的输出示例]
```

### 最佳实践

✅ **推荐做法**：
- 使用明确的变量名（`chapterTitle` 而非 `title`）
- 为可选变量使用条件块（`{{#if}}...{{/if}}`）
- 在注释中说明变量用途
- 保持模板简洁，避免冗长

❌ **避免做法**：
- 在模板中硬编码业务逻辑
- 使用过于复杂的嵌套条件
- 忽略空值处理
- 模板过大（> 2KB）

---

## 热拔插机制实现

### 方案一：开发时热更新（Vite HMR）

**适用场景**：开发调试阶段

**实现方式**：

```typescript
// src/prompts/TemplateRenderer.ts
if (import.meta.hot) {
  import.meta.hot.accept('../prompts/templates/**/*.md', (newModule) => {
    // 模板文件变更时清除缓存
    console.log('🔥 模板已更新，清除缓存');
    templateRenderer.clearCache();
  });
}
```

**使用方法**：
1. 修改 `src/prompts/templates/` 下的 `.md` 文件
2. Vite 自动检测变更
3. 页面热更新，无需刷新浏览器

### 方案二：生产时用户自定义目录

**适用场景**：用户个性化配置

**实现方式**（需要 Tauri 文件系统 API）：

```typescript
// src/prompts/TemplateRenderer.ts
import { readTextFile, BaseDirectory } from '@tauri-apps/plugin-fs';
import { appDataDir } from '@tauri-apps/api/path';

export class TemplateRenderer {
  private async getUserTemplatePath(filePath: string): Promise<string | null> {
    try {
      const appDataPath = await appDataDir();
      const userTemplatePath = `${appDataPath}/prompts/${filePath}`;
      return await readTextFile(userTemplatePath);
    } catch {
      return null; // 用户目录不存在或文件不存在
    }
  }

  async loadTemplate(templateId: string): Promise<string> {
    const config = PROMPT_TEMPLATES[templateId];

    // 🆕 优先加载用户自定义模板
    const userTemplate = await this.getUserTemplatePath(config.file);
    if (userTemplate) {
      console.log(`✅ 使用用户自定义模板: ${config.file}`);
      return userTemplate;
    }

    // 回退到内置模板
    return await this.getBuiltInTemplate(config.file);
  }
}
```

**用户自定义目录结构**：

```
~/.writing-studio/prompts/
└── templates/
    └── pipeline/
        ├── outline-generate.md   # 用户自定义版本
        └── chapter-generate.md   # 用户自定义版本
```

**优先级**：
```
用户自定义模板 > 内置模板
```

### 方案三：运行时切换模板版本

**适用场景**：A/B 测试、灰度发布

**实现方式**：

```typescript
// src/prompts/index.ts
export const PROMPT_TEMPLATES = {
  'outline-generate': {
    id: 'outline-generate',
    versions: {
      'v1': './templates/pipeline/outline-generate-v1.md',
      'v2': './templates/pipeline/outline-generate-v2.md',
    },
    activeVersion: 'v1', // 可通过配置切换
  },
};

// 使用示例
const userMessage = await composer.renderTemplate(
  'outline-generate',
  variables,
  'v2' // 指定使用 v2 版本
);
```

---

## 质量保证与回滚

### 版本控制策略

#### 1. 模板版本管理

在每个模板文件中添加元数据注释：

```markdown
---
id: outline-generate
version: 1.2.0
author: AI Team
created: 2026-05-31
updated: 2026-06-15
changelog:
  - 1.2.0: 增加悬念线生成要求
  - 1.1.0: 优化格式示例
  - 1.0.0: 初始版本
---

# 角色
...
```

#### 2. 回滚机制

```typescript
// src/prompts/TemplateRenderer.ts
export class TemplateRenderer {
  private versionHistory = new Map<string, string[]>();

  // 加载指定版本
  async loadTemplateVersion(templateId: string, version: string): Promise<string> {
    const filePath = `./templates/pipeline/${templateId}-v${version}.md`;
    return await this.loadBuiltInTemplate(filePath);
  }

  // 回滚到上一个版本
  async rollback(templateId: string): Promise<void> {
    const history = this.versionHistory.get(templateId);
    if (history && history.length > 1) {
      const previousVersion = history[history.length - 2];
      await this.loadTemplateVersion(templateId, previousVersion);
    }
  }
}
```

### 测试策略

#### 1. 单元测试

```typescript
// src/prompts/__tests__/TemplateRenderer.test.ts
import { TemplateRenderer } from '../TemplateRenderer';

describe('TemplateRenderer', () => {
  it('should replace simple variables', () => {
    const renderer = new TemplateRenderer();
    const template = '题材: {{genres}}';
    const result = renderer.render(template, { genres: '玄幻' });
    expect(result).toBe('题材: 玄幻');
  });

  it('should handle conditional blocks', () => {
    const renderer = new TemplateRenderer();
    const template = '{{#if custom}}**额外**: {{custom}}{{/if}}';
    
    const result1 = renderer.render(template, { custom: '要求' });
    expect(result1).toBe('**额外**: 要求');
    
    const result2 = renderer.render(template, {});
    expect(result2).toBe('');
  });
});
```

#### 2. 集成测试

```typescript
// 测试完整流程
describe('Pipeline Outline Generation', () => {
  it('should generate outline using template', async () => {
    const service = new NovelLLMService();
    service.init(prompts, config);
    
    const result = await service.generatePipelineOutline({
      genres: ['玄幻'],
      plotType: '逆袭',
      protagonistIdentity: '废柴少年',
      tone: '热血',
      customPrompt: '',
    });
    
    expect(result).toContain('基本信息');
    expect(result).toContain('章节规划');
  });
});
```

### 监控与日志

```typescript
// 添加提示词使用日志
class NovelLLMService {
  private usageLog: Array<{
    template: string;
    variables: Record<string, any>;
    timestamp: number;
    success: boolean;
  }> = [];

  async generatePipelineOutline(config: PipelineStep1Config): Promise<string> {
    const startTime = Date.now();
    try {
      const userMessage = await this.composer!.renderTemplate('outline-generate', {
        // ...
      });
      
      const result = await this.generate('PLANNING', userMessage, { ... });
      
      this.usageLog.push({
        template: 'outline-generate',
        variables: config,
        timestamp: startTime,
        success: true,
      });
      
      return result;
    } catch (error) {
      this.usageLog.push({
        template: 'outline-generate',
        variables: config,
        timestamp: startTime,
        success: false,
      });
      throw error;
    }
  }

  // 导出使用日志
  exportUsageLog(): string {
    return JSON.stringify(this.usageLog, null, 2);
  }
}
```

---

## 常见问题解答

### Q1: 改造会影响性能吗？

**A**: 不会。模板加载使用缓存机制：
- 首次加载：约 50ms（从文件系统读取）
- 缓存命中：< 5ms
- 模板渲染：< 10ms（简单的字符串替换）

### Q2: 如果模板文件丢失怎么办？

**A**: 系统会抛出明确的错误信息：
```
Error: 未找到模板文件: outline-generate.md
```
建议在应用启动时验证所有模板文件存在性。

### Q3: 如何在不同环境使用不同模板？

**A**: 使用环境变量或配置文件：
```typescript
// .env.development
VITE_PROMPT_TEMPLATES_DIR=./src/prompts/templates

// .env.production
VITE_PROMPT_TEMPLATES_DIR=./dist/prompts/templates
```

### Q4: 用户如何自定义提示词？

**A**: 提供 UI 界面或文档指引：
1. 打开用户数据目录：`~/.writing-studio/prompts/`
2. 复制内置模板到该目录
3. 编辑自定义版本
4. 重启应用生效

### Q5: 支持多语言提示词吗？

**A**: 当前版本仅支持中文，但架构支持扩展：
```
src/prompts/
├── templates/
│   ├── zh-CN/
│   │   └── outline-generate.md
│   └── en-US/
│       └── outline-generate.md
```

### Q6: 如何调试提示词？

**A**: 三种方式：
1. **浏览器控制台**：查看 `console.log` 输出的渲染后提示词
2. **PromptPreview 面板**：在 UI 中预览最终发送给 LLM 的提示词
3. **导出日志**：使用 `exportUsageLog()` 导出完整的使用记录

---

## 后续优化方向

### 短期（1-2 周）
- [ ] 完成所有模板编写和测试
- [ ] 添加模板验证工具（检查语法、变量完整性）
- [ ] 编写用户自定义提示词文档

### 中期（1-2 月）
- [ ] 实现 Tauri 文件系统集成的热拔插
- [ ] 添加提示词 A/B 测试框架
- [ ] 建立提示词效果评估机制（用户评分、成功率）

### 长期（3-6 月）
- [ ] 支持社区提示词共享平台
- [ ] 集成 AI 辅助提示词生成
- [ ] 实现提示词版本自动回滚

---

## 附录

### A. 变量速查表

| 变量名 | 类型 | 说明 | 使用场景 |
|--------|------|------|----------|
| `genres` | string | 题材类型（逗号分隔） | outline-generate |
| `plotType` | string | 剧情类型 | outline-generate |
| `protagonistIdentity` | string | 主角身份 | outline-generate |
| `tone` | string | 基调 | outline-generate |
| `customPrompt` | string | 自定义要求 | outline-generate |
| `originalOutline` | string | 原始大纲 | outline-refine |
| `currentOutline` | string | 当前大纲 | outline-refine |
| `historyLines` | string | 历史修改记录 | outline-refine |
| `additions` | string | 新增内容 | refine 系列 |
| `deletions` | string | 删除内容 | refine 系列 |
| `modifications` | string | 修改内容 | refine 系列 |
| `outline` | string | 粗纲内容 | detailed-generate |
| `chapterCount` | number | 章节数量 | detailed-generate |
| `chapterTitle` | string | 章节标题 | chapter-generate |
| `chapterOutline` | string | 章节细纲 | chapter-generate |
| `previousChapterContent` | string | 上一章正文 | chapter-generate |
| `writingStyle` | string | 写作风格 | chapter-generate |
| `storyLength` | string | 篇幅要求 | chapter-generate |
| `customRules` | string | 自定义规则 | chapter-generate |

### B. 模板文件清单

| 文件名 | 对应方法 | 阶段 | 状态 |
|--------|----------|------|------|
| `outline-generate.md` | `generatePipelineOutline` | Step2 | ☐ 待创建 |
| `outline-refine.md` | `refinePipelineOutline` | Step2 | ☐ 待创建 |
| `detailed-generate.md` | `generatePipelineDetailedOutline` | Step4 | ☐ 待创建 |
| `detailed-refine.md` | `refinePipelineDetailedOutline` | Step4 | ☐ 待创建 |
| `detailed-refine-chapter.md` | `refinePipelineDetailedOutlineChapter` | Step4 | ☐ 待创建 |
| `chapter-generate.md` | `generatePipelineChapter` | Step5 | ☐ 待创建 |
| `chapter-refine.md` | `refinePipelineChapter` | Step5 | ☐ 待创建 |

### C. 相关文件索引

| 文件 | 路径 | 说明 |
|------|------|------|
| 组件入口 | `src/components/PipelineWriting/index.tsx` | 流水线写作主组件 |
| Step2 UI | `src/components/PipelineWriting/Step2Outline.tsx` | 大纲生成界面 |
| Step4 UI | `src/components/PipelineWriting/Step4DetailedOutline.tsx` | 细纲生成界面 |
| Step5 UI | `src/components/PipelineWriting/Step5WriteText.tsx` | 正文生成界面 |
| LLM 服务 | `src/llm/NovelLLMService.ts` | 提示词调用逻辑 |
| 提示词组合 | `src/hooks/SmartPromptComposer.ts` | 现有提示词引擎 |
| 提示词索引 | `src/hooks/prompts-index.ts` | 参考文档映射 |
| 提示词构建 | `src/hooks/promptBuilders.ts` | 上下文构建器 |

---

## 总结

通过本指南的实施，你将实现：

✅ **灵活性**：修改提示词无需重新编译代码  
✅ **可维护性**：提示词与业务逻辑解耦  
✅ **可扩展性**：支持用户自定义和 A/B 测试  
✅ **可追溯性**：版本控制和回滚机制  
✅ **团队协作**：非开发人员也可参与提示词优化  

**下一步行动**：
1. 从第一步开始，创建目录结构
2. 逐步实施，每步完成后进行验证
3. 遇到问题时参考本指南的"常见问题解答"部分

祝你改造顺利！🚀
